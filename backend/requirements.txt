# GoalPilot AI backend — production deps
# No emergentintegrations (private package) — uses official SDKs directly.

# FastAPI stack
fastapi==0.110.1
uvicorn[standard]==0.25.0
pydantic[email]>=2.6.0,<3.0.0

# Auth & security
python-jose[cryptography]==3.3.0
PyJWT==2.10.1
bcrypt==4.1.3
passlib[bcrypt]==1.7.4

# Environment
python-dotenv==1.0.1

# Database
motor==3.3.1
pymongo==4.5.0

# HTTP
httpx==0.28.1
requests==2.32.5

# AI
anthropic==0.100.0

# Payments
stripe==15.0.1

# Email — plain httpx call, no SDK required

# Scheduler & timezone
APScheduler==3.11.2
tzlocal==5.3

# Testing
pytest==8.4.2
pytest-asyncio==0.24.0
